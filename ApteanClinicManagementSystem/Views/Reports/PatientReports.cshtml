@model IEnumerable<ApteanClinicManagementSystem.Models.PatientReport>

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>PatientReports</title>
</head>
<body>
    <h2>@ViewBag.Title</h2>
    <hr />
    @*@if (ViewBag.ReportType == 1)
    {
        SAction = "PatientOutStandingReports";
    }
    else
    {
        SAction = "PatientsAllInvoice";
    }
    @using (Html.BeginForm(SAction, "Reports", FormMethod.Post))
    {
        <label>Search Patient Name :</label>@Html.TextBox("PatientName")
        <input type="submit" value="Search" />
    }
    <br />*@
    <table id="pageTable" class="table table-striped table-bordered" style="width:100%">
        <thead>
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.InvoiceNumber)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Name)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Total)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.InvoiceDate)
                </th>
                <th>@Html.Label("Action")</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.InvoiceNumber)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Name)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Total)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.InvoiceDate)
                    </td>
                    <td>
                        <a href="@Url.Action("View","Invoice",new { invoiceId = item.InvoiceNumber })">
                            <span class="glyphicon glyphicon-eye-open"></span>
                        </a>
                    </td>
                </tr>
                @Html.HiddenFor(modelItem => item.PatientId)
            }
        </tbody>

    </table>
</body>
</html>
